<UserControl x:Class="PhoneBook.Views.ContactEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="30">
        <StackPanel MaxWidth="400" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding TitleText}" FontSize="22" FontWeight="Bold" Margin="0,0,0,25" HorizontalAlignment="Center" Foreground="#2C3E50"/>

            <TextBlock Text="Имя контакта:" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
            <TextBox Text="{Binding NameInput, UpdateSourceTrigger=PropertyChanged}" Height="35" Margin="0,0,0,15" Padding="5,0" VerticalContentAlignment="Center" FontSize="14"/>

            <TextBlock Text="Номер телефона:" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
            <TextBox Text="{Binding PhoneInput, UpdateSourceTrigger=PropertyChanged}" Height="35" Margin="0,0,0,25" Padding="5,0" VerticalContentAlignment="Center" FontSize="14"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="💾 Сохранить" Command="{Binding SaveCommand}" Grid.Column="0" Height="40" Margin="0,0,10,0" Background="#2ECC71" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                <Button Content="❌ Отмена" Command="{Binding CancelCommand}" Grid.Column="1" Height="40" Margin="10,0,0,0" Background="#95A5A6" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>